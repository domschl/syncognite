#include "cp-math.h"
#include "cp-random.h"
/*
template <typename T>
void Matrix<T>::randomNormal(T mean, T std) {
    Random r;
    for (auto i=0; i<m.size(); i++) {
        m[i]=r.normal(mean, std);
    }
}

template <typename T>
void Matrix<T>::random(T a, T b) {
    Random r;
    for (auto i=0; i<m.size(); i++) {
        m[i]=r.floatrange(a,b);
    }
}

template <typename T>
T Matrix<T>::sum() {
    T s=0;
    for (auto i=0; i<m.size(); i++) {
        s += m[i];
    }
    return s;
}
*/
